# vyučtování — Vite + React + TypeScript (demo)

Minimalní scaffold s MUI a Zustand pro rychlé startování.

### Spuštění na lokálu

Instaluj závislosti a spusť dev server:

```bash
npm install
npm run dev
```

Pro výrobu (build):

```bash
npm run build
npm run preview
```